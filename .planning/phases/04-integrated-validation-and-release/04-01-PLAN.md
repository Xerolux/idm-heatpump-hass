---
phase: 04-integrated-validation-and-release
plan: "01"
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - tests/test_release_contract.py
  - tests/test_cross_repo_contract.py
  - tests/test_init.py
  - tests/test_services.py
  - tests/test_entity.py
  - tests/test_platforms.py
  - tests/test_coordinator.py
  - tests/test_diagnostics.py
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "The complete milestone passes pytest tests/, strict mypy on custom_components/idm_heatpump, ruff check + format --check, and the cross-repository and release-contract tests together."
    - "The release-contract pin and CI workflow expectations reflect the actual manifest and CI files (updated only if a real dependency change occurred in Phase 3)."
    - "No regression in the existing cross-repo register/contract suites for Navigator 2.0 and Navigator 10."
  artifacts:
    - path: tests/test_release_contract.py
      provides: contract expectations consistent with manifest.json and CI (updated only if Phase 3 changed the API pin)
    - path: tests/ (aggregate)
      provides: green integrated suite covering lifecycle, writable sentinel, diagnostics, and contracts
  key_links:
    - from: tests/test_release_contract.py
      to: manifest.json
      via: "EXPECTED_RUNTIME_REQUIREMENTS == manifest['requirements']"
      pattern: "idm-heatpump-api\\[web\\]=="
---

<objective>
Prove the whole milestone together: every automated gate (pytest, strict mypy, ruff, cross-repo
contract, release contract) is green and mutually consistent.

Purpose: Each phase was verified in isolation; before release we must prove the fixes compose
without regression and that the release contract still matches the shipped manifest and CI.
Output: a single green integrated run plus any contract-expectation sync needed.
</objective>

<execution_context>
@.planning/phases/04-integrated-validation-and-release/04-01-PLAN.md
@.planning/phases/04-integrated-validation-and-release/04-01-SUMMARY.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PLAN.md
@manifest.json
@pytest.ini
@mypy.ini
@ruff.toml
@tests/test_release_contract.py
@tests/test_cross_repo_contract.py
</context>

<interfaces>
Quality gates: `pytest` (pytest.ini: testpaths=tests, asyncio_mode=auto, fully stubbed HA/pymodbus/
idm_heatpump via conftest.py); `mypy custom_components/idm_heatpump` (mypy.ini strict with the
existing disable_error_code allow-list); `ruff check custom_components/idm_heatpump tests` and
`ruff format ... --check` (ruff.toml line-length 120). Release contract: `EXPECTED_RUNTIME_REQUIREMENTS`
(test_release_contract.py:16-19) must equal `manifest.json` requirements; CI workflow assertions in
the same file. Cross-repo contract: `tests/test_cross_repo_contract.py` builds Nav10/Nav20 maps.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 04-01-01: Run the integrated gate and sync contract expectations</name>
  <files>tests/test_release_contract.py, tests/test_cross_repo_contract.py</files>
  <read_first>
    - tests/test_release_contract.py
    - tests/test_cross_repo_contract.py
    - manifest.json
    - pytest.ini
    - mypy.ini
    - ruff.toml
  </read_first>
  <action>
Run the full integrated suite in order and capture results:

1. `pytest tests/ -q` — expect 0 failures.
2. `mypy custom_components/idm_heatpump` — expect success under the existing strict allow-list.
3. `ruff check custom_components/idm_heatpump tests` and
   `ruff format custom_components/idm_heatpump tests --check` — expect clean.

If Phase 3 (category D) changed the `idm-heatpump-api` pin in `manifest.json`, update
`EXPECTED_RUNTIME_REQUIREMENTS` in `tests/test_release_contract.py` (lines 16-19) so it equals the
new `manifest.json` requirements exactly (including the unchanged `pymodbus>=3.12.1,<4.0` entry).
If no pin changed, do NOT edit the expectations. Then re-run the contract tests to confirm they
match the actual manifest and CI files.

Confirm `tests/test_cross_repo_contract.py` still builds Navigator 2.0 and Navigator 10 maps with
no duplicates, correct description keys, writable registers on write-capable platforms, and Nav20
excluding `_NAVIGATOR_10_ONLY_REGISTERS`.
  </action>
  <acceptance_criteria>
    - `pytest tests/ -q` exits 0.
    - `mypy custom_components/idm_heatpump` exits 0.
    - `ruff check custom_components/idm_heatpump tests` and `ruff format ... --check` exit 0.
    - `EXPECTED_RUNTIME_REQUIREMENTS` equals `manifest.json` requirements exactly.
    - `tests/test_cross_repo_contract.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/ -q && mypy custom_components/idm_heatpump && ruff check custom_components/idm_heatpump tests && ruff format custom_components/idm_heatpump tests --check</automated>
  </verify>
  <done>The integrated milestone is green across pytest, mypy, ruff, and both contract suites, with expectations synced to the manifest.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                              | Purpose                                                          |
| ------------------------------------------- | ---------------------------------------------------------------- |
| synced `EXPECTED_RUNTIME_REQUIREMENTS`        | Keeps release-contract test aligned with the shipped manifest    |
| integrated green run evidence                | Proves the milestone composes without regression                 |

</artifacts_produced>

<verification>
The task command above is the verification. Record the exact pytest/mypy/ruff results in
04-01-SUMMARY.md.
</verification>

<success_criteria>

- QUAL-02: the milestone passes pytest tests/, strict mypy, ruff, and the cross-repo + release-contract tests.
- Contract expectations are consistent with manifest.json and CI (no drift).
</success_criteria>

<output>Create `.planning/phases/04-integrated-validation-and-release/04-01-SUMMARY.md` when done, including the recorded gate results.</output>
