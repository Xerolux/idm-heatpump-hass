---
phase: 04-integrated-validation-and-release
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - docs/CHANGELOG.md
  - custom_components/idm_heatpump/manifest.json
  - .planning/STATE.md
autonomous: false
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "Changelog and GitHub issues document cause, user impact, workaround, verification evidence, and the relationship among #170, #171, and #172."
    - "A release is created only after the integrated automated gate is green and the model portion of #170 is either field-verified or explicitly bounded as still open."
    - "The manifest version is bumped only for an actual release, and the release ZIP + SHA-256 are checked per the existing release contract."
    - "The boundary of #170's model part is explicit: either verified-fixed or a documented open checkpoint — never silently unresolved."
  artifacts:
    - path: docs/CHANGELOG.md
      provides: an entry under ## [Unreleased] (or a versioned section at release) with Fixed/Changed subsections for #170/#171/#172
    - path: custom_components/idm_heatpump/manifest.json
      provides: version bump only at release time (e.g. 0.8.6), with API pin unchanged unless Phase 3 category D applied
    - path: .planning/STATE.md
      provides: milestone progress advanced to reflect release readiness
  key_links:
    - from: docs/CHANGELOG.md
      to: tests/test_release_contract.py
      via: "release contract enforces expected_heading == f## [{version}]"
      pattern: "## \\[Unreleased\\]|### Fixed"
---

<objective>
Close the milestone with reproducible release evidence: documentation, issue records, and a release
that ships only when every gate passes and the #170 model boundary is explicit.

Purpose: Users and maintainers must be able to understand what was fixed, how it was verified, and
what (if anything) remains open. Output: changelog + issue comments + a bounded release decision.
</objective>

<execution_context>
@.planning/phases/04-integrated-validation-and-release/04-02-PLAN.md
@.planning/phases/04-integrated-validation-and-release/04-02-SUMMARY.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PLAN.md
@docs/CHANGELOG.md
@manifest.json
@tests/test_release_contract.py
</context>

<interfaces>
Changelog format: `## [Unreleased]` then `## [VERSION] - YYYY-MM-DD` with `### Added/Changed/Fixed`
subsections (release contract enforces `expected_heading = f"## [{version}]"`). Release flow:
GitHub Actions `release.yml` on `v*` tags builds `idm_heatpump.zip` from
`custom_components/idm_heatpump/`; `hacs.json` sets `filename`/`zip_release`. Manifest `version`
today is `0.8.5`; API pin is `idm-heatpump-api[web]==0.8.4`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 04-02-01: Write changelog and GitHub issue documentation</name>
  <files>docs/CHANGELOG.md</files>
  <read_first>
    - docs/CHANGELOG.md
    - .planning/phases/01-service-lifecycle/01-01-SUMMARY.md
    - .planning/phases/02-writable-sentinel-availability/02-01-SUMMARY.md
    - .planning/phases/02-writable-sentinel-availability/02-02-SUMMARY.md
    - .planning/phases/03-model-conflict-diagnosis/03-01-SUMMARY.md
    - .planning/phases/03-model-conflict-diagnosis/03-02-SUMMARY.md
    - tests/test_release_contract.py
  </read_first>
  <action>
Under `## [Unreleased]` in `docs/CHANGELOG.md`, add a `### Fixed` subsection with three entries:

- #171: Domain services (`set_external_climate`, `set_system_mode`, `acknowledge_errors`,
  `write_register`) now survive config-entry reload/unload and options changes; no HA core restart
  required. State cause, impact, workaround (previously: core restart), and verification
  (lifecycle-invariant tests).
- #172 (+ GLT setpoint part of #170): Writable GLT/PV/steering targets remain addressable when
  their value is an unset sentinel, reporting unknown state instead of a fabricated value, while
  read-only and unsupported registers stay filtered. Note the workaround no longer needed
  (`hide_unused_registers: false`).
- #170 model part: state the outcome from plan 03-02 — either field-verified fixed, or explicitly
  documented as an open, evidence-gated checkpoint with the required reporter data listed.

If an API pin bump occurred (Phase 3 category D), add a `### Changed` entry naming the new exact
`idm-heatpump-api` version and reason. Do not invent a version heading yet (added at release).
  </action>
  <acceptance_criteria>
    - `docs/CHANGELOG.md` contains a `### Fixed` block under `## [Unreleased]` referencing #171, #172, and #170.
    - Each entry names cause, user impact, workaround, and verification evidence.
    - The #170 model outcome is explicitly either fixed or an open checkpoint (not silent).
    - If the API pin changed, a `### Changed` entry exists with the exact version; otherwise no Changed entry is added.
  </acceptance_criteria>
  <verify>
    <manual>Reviewer confirms the three issue relationships and evidence are accurately described.</manual>
  </verify>
  <done>The changelog explains cause/impact/workaround/verification and the #170/#171/#172 relationship.</done>
</task>

<task type="checkpoint" autonomous="false">
  <name>Task 04-02-02: Release decision gate and version bump</name>
  <files>custom_components/idm_heatpump/manifest.json, .planning/STATE.md</files>
  <read_first>
    - manifest.json
    - hacs.json
    - tests/test_release_contract.py
    - .planning/STATE.md
    - docs/CHANGELOG.md
  </read_first>
  <action>
Verify the release gate BEFORE any version bump:

1. The integrated automated gate from 04-01 is green (pytest, mypy, ruff, cross-repo + release
   contract).
2. #171 and #172 are fully reproduced and fixed.
3. The GLT/setpoint part of #170 is verified.
4. The model part of #170 is EITHER field-verified fixed OR explicitly bounded as still open in
   the changelog and in issue #170.

Only if the gate passes: bump `manifest.json` `version` (e.g. `0.8.5` -> `0.8.6`), convert the
changelog `## [Unreleased]` section into `## [0.8.6] - YYYY-MM-DD`, and confirm
`tests/test_release_contract.py` accepts the new heading. Leave the `idm-heatpump-api` pin unchanged
unless Phase 3 category D already bumped it. After the tag/release, confirm the release ZIP and
SHA-256 checks per the existing release contract (`release.yml` + `test_release_contract.py`).

If the gate does not pass (e.g. #170 model evidence still insufficient), do NOT bump the version;
record the blocker in STATE.md and keep the milestone open.
  </action>
  <acceptance_criteria>
    - Release gate criteria 1-4 are each explicitly satisfied and recorded in 04-02-SUMMARY.md before any bump.
    - If released: `manifest.json` version is bumped, changelog has a dated `## [X.Y.Z]` heading, and `pytest tests/test_release_contract.py -q` exits 0.
    - If not released: no version bump occurred and STATE.md records the open blocker.
    - Release ZIP + SHA-256 verified (if released) per the existing release contract.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_release_contract.py -q</automated>
    <manual>Reviewer confirms the release gate and (if applicable) ZIP/SHA-256 evidence.</manual>
  </verify>
  <done>A reproducible bugfix release ships only when every gate passes and the #170 model boundary is explicit, or the milestone stays open with a recorded blocker.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                          | Purpose                                                              |
| --------------------------------------- | -------------------------------------------------------------------- |
| `docs/CHANGELOG.md` milestone entries   | Cause/impact/workaround/verification for #170/#171/#172              |
| `manifest.json` version bump (release)  | Marks the bounded bugfix release                                     |
| release ZIP + SHA-256 evidence          | Reproducible distribution per the existing release contract          |
| `.planning/STATE.md` release readiness  | Records gate outcome and any open blocker                            |

</artifacts_produced>

<verification>
`pytest tests/test_release_contract.py -q`; plus manual confirmation of the release gate criteria
and (if released) the ZIP/SHA-256 evidence.
</verification>

<success_criteria>

- REL-01: changelog and GitHub issues document cause, impact, workaround, verification, and the #170/#171/#172 relationship.
- REL-02: a release is created only after the automated gate is green and the model part of #170 is field-verified or explicitly bounded as open.
- The milestone either ships as a bounded, reproducible release or stays open with a recorded, evidence-gated blocker.
</success_criteria>

<output>Create `.planning/phases/04-integrated-validation-and-release/04-02-SUMMARY.md` when done, recording the release decision and evidence.</output>
