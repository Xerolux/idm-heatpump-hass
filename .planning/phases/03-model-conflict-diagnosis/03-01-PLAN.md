---
phase: 03-model-conflict-diagnosis
plan: "01"
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - custom_components/idm_heatpump/diagnostics.py
  - custom_components/idm_heatpump/coordinator.py
  - tests/test_diagnostics.py
  - tests/test_web_data.py
autonomous: true
requirements: [MODEL-01, MODEL-02]
must_haves:
  truths:
    - "After Phase 2, the missing PV-surplus, PV-production, and household-consumption setpoints from #170 are retested and classified as either resolved by the #172 path or documented as a distinct remaining defect."
    - "A conflicting-model diagnostic emits source, selected Navigator family, web variant, firmware evidence, and any active manual override, without exposing host, PIN, serial number, or other private connection data."
    - "Diagnostics never include myIDM serial number or raw web auth material; the existing TO_REDACT set is extended to cover any newly exposed identifier."
    - "No model-selection correction is applied in this plan; correction is reserved for plan 03-02 and only with redacted field evidence."
  artifacts:
    - path: custom_components/idm_heatpump/diagnostics.py
      provides: a structured model_conflict diagnostic block with sources/family/web_variant/firmware/override, redacted
    - path: custom_components/idm_heatpump/coordinator.py
      provides: a read-only model-conflict summary derived from existing detection fields (no new write/selection)
    - path: tests/test_diagnostics.py
      provides: tests asserting the conflict block is present, structured, and fully redacted
    - path: tests/test_web_data.py
      provides: tests asserting merge_model_info / web variant classification inputs are stable
  key_links:
    - from: custom_components/idm_heatpump/diagnostics.py
      to: custom_components/idm_heatpump/__init__.py
      via: "detection reconciliation at __init__.py:605-803 exposes a conflict summary"
      pattern: "navigator_family|CONF_DETECTED_NAVIGATOR_VERSION|CONF_MODEL_OVERRIDE"
---

<objective>
Diagnose the model half of #170 without guessing a Navigator family: re-verify the missing
setpoints after the #172 fix, then add a structured, fully redacted model-conflict diagnostic.

Purpose: The same writable-setpoint mechanism often hides PV/GLT targets; once #172 is in, we must
confirm whether #170's missing entities are gone or are a real detection defect. A reproducible,
redacted diagnostic is the evidence required before any correction. Output: classification +
diagnostics only — no model-selection change here.
</objective>

<execution_context>
@.planning/phases/03-model-conflict-diagnosis/03-01-PLAN.md
@.planning/phases/03-model-conflict-diagnosis/03-01-SUMMARY.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PLAN.md
@custom_components/idm_heatpump/__init__.py
@custom_components/idm_heatpump/coordinator.py
@custom_components/idm_heatpump/web_data.py
@custom_components/idm_heatpump/diagnostics.py
@custom_components/idm_heatpump/adapter_registers.py
@tests/test_diagnostics.py
@tests/test_web_data.py
</context>

<interfaces>
Detection fields live in `entry.data`: `CONF_DETECTED_NAVIGATOR_VERSION`, `CONF_DETECTED_SOFTWARE_VERSION`,
`CONF_DETECTED_WEB_VARIANT`, and `CONF_MODEL_OVERRIDE`. Reconciliation happens in `async_setup_entry`
(__init__.py:605-803) and at runtime in `coordinator.async_refresh_web_supplement` (coordinator.py:669)
via `merge_model_info` (web_data.py:624) and `_firmware_indicates_nav10` (web_data.py:611).
Diagnostics redaction is `TO_REDACT` (diagnostics.py:19); it currently does NOT include the myIDM
serial/identifier. `_NAVIGATOR_10_ONLY_REGISTERS` (adapter_registers.py:21) is the family-specific
register set.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-01-01: Re-verify the #170 missing setpoints after the #172 fix</name>
  <files>tests/test_platforms.py, tests/test_entity.py</files>
  <read_first>
    - tests/test_platforms.py
    - tests/test_entity.py
    - custom_components/idm_heatpump/entity.py
    - custom_components/idm_heatpump/coordinator.py
    - custom_components/idm_heatpump/adapter_registers.py
  </read_first>
  <action>
Add a classification-oriented test that, given a model-supported configuration where the PV-surplus,
PV-production, and household-consumption registers are writable and present in the poll dataset,
asserts the corresponding writable entities are created and remain available-but-None-stated when
their values are sentinels (the exact #170 scenario, now exercising the Phase-2 creation +
availability path). Then add a counterpart test for the case where those registers are NOT part of
the detected model map and assert they are NOT created (so a detection defect would still be
visible as missing entities, separable from the #172 filter defect).

Record the classification outcome as an assertion comment / test name so the result is captured in
the test suite: e.g. `test_pv_surplus_setpoint_present_when_model_supported` (passes after #172) and
`test_pv_surplus_setpoint_absent_when_model_excluded` (characterizes the detection-defect path).
Do not modify model selection in this task.
  </action>
  <acceptance_criteria>
    - A test proves model-supported PV-surplus/PV-production/household-consumption writable entities are created and available-but-None when sentinel (the #172 path resolves the #170 setpoints for supported models).
    - A test proves the same entities are absent when the registers are not in the detected model map (detection defect remains observable and separable).
    - No production model-selection code is changed in this task.
    - `pytest tests/test_platforms.py tests/test_entity.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_platforms.py tests/test_entity.py -q</automated>
  </verify>
  <done>#170's setpoint symptom is classified as resolved-by-#172 (supported models) versus a distinct detection defect (excluded models), captured in tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-02: Add a structured, redacted model-conflict diagnostic</name>
  <files>custom_components/idm_heatpump/diagnostics.py, custom_components/idm_heatpump/coordinator.py, tests/test_diagnostics.py</files>
  <read_first>
    - custom_components/idm_heatpump/diagnostics.py
    - custom_components/idm_heatpump/coordinator.py
    - custom_components/idm_heatpump/__init__.py
    - custom_components/idm_heatpump/web_data.py
    - tests/test_diagnostics.py
  </read_first>
  <action>
In `custom_components/idm_heatpump/coordinator.py`, expose a read-only summary computed from
existing fields only, e.g. a property `model_conflict_summary` returning a dict with keys like
`modbus_family`, `stored_family`, `web_variant`, `software_version_prefix`,
`manual_override` (from `entry.data[CONF_MODEL_OVERRIDE]`), and a boolean `conflict` derived from
comparing `navigator_family` of stored vs fresh Modbus model name (reuse the comparison already done
at __init__.py:641-649). Do NOT change model selection, do NOT persist anything new, do NOT add a
write path.

In `custom_components/idm_heatpump/diagnostics.py`:

1. Add the myIDM identifier / serial keys to `TO_REDACT` (diagnostics.py:19) so any newly exposed
   device identifier is redacted (today it is NOT).
2. In `async_get_config_entry_diagnostics` (diagnostics.py:80), add a `model_conflict` block built
   from `coordinator.model_conflict_summary`, containing only: selected family, stored family,
   web variant, firmware evidence (prefix/NAV10 indicator), and manual override. No host, PIN,
   serial, or auth token. Reuse `_sanitized_web_error` (diagnostics.py:56) for any error string.

Add tests in `tests/test_diagnostics.py`:
- `test_model_conflict_block_present_and_structured`: asserts the block keys exist.
- `test_model_conflict_block_redacts_private_data`: asserts host/port/slave_id/web_host/web_pin AND
  the myIDM/serial identifier are absent from the diagnostic output (extend the existing
  `test_sensitive_fields_redacted` case at test_diagnostics.py:86).
- `test_model_conflict_block_reflects_override_and_web_variant`: asserts the manual override and
  web variant appear and match the entry data.
  </action>
  <acceptance_criteria>
    - `coordinator.model_conflict_summary` is read-only and derives only from existing detection fields (no new persistence, no write path).
    - Diagnostics output contains a `model_conflict` block with selected/stored family, web variant, firmware evidence, and manual override.
    - Diagnostics output does NOT contain host, port, slave_id, web_host, web_pin, myIDM id, or serial number (assert each is absent).
    - `pytest tests/test_diagnostics.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_diagnostics.py tests/test_web_data.py -q</automated>
  </verify>
  <done>A reproducible, fully redacted model-conflict diagnostic is available to classify #170 evidence; no model selection was changed.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                                  | Purpose                                                                  |
| ----------------------------------------------- | ------------------------------------------------------------------------ |
| `IdmCoordinator.model_conflict_summary`          | Read-only conflict summary from existing detection fields                |
| diagnostics `model_conflict` block               | Structured, redacted evidence for #170 classification                    |
| extended `TO_REDACT` (myIDM/serial)              | Closes the existing redaction gap for device identifiers                 |
| #170 setpoint classification tests               | Separates #172-resolved cases from genuine detection defects             |

</artifacts_produced>

<verification>
Run `pytest tests/test_diagnostics.py tests/test_web_data.py tests/test_platforms.py tests/test_entity.py -q`,
then `mypy custom_components/idm_heatpump`, then `ruff check custom_components/idm_heatpump tests`
and `ruff format custom_components/idm_heatpump tests --check`.
</verification>

<success_criteria>

- MODEL-01: the #170 missing setpoints are retested after Phase 2 and documented as either resolved by #172 or a distinct remaining defect.
- MODEL-02: a conflicting-model diagnostic identifies Modbus/stored/web sources, selected family, web variant, firmware evidence, and manual override, without exposing private data.
- No model-selection correction is applied in this plan (reserved for 03-02 with evidence).
</success_criteria>

<output>Create `.planning/phases/03-model-conflict-diagnosis/03-01-SUMMARY.md` when done. If field evidence of a real detection defect is captured, proceed to plan 03-02; otherwise document MODEL-03/MODEL-04 as an open, evidence-gated checkpoint in the summary and in the GitHub issue.</output>
