---
phase: 03-model-conflict-diagnosis
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - custom_components/idm_heatpump/__init__.py
  - custom_components/idm_heatpump/coordinator.py
  - custom_components/idm_heatpump/web_data.py
  - custom_components/idm_heatpump/adapter_registers.py
  - manifest.json
  - tests/test_init.py
  - tests/test_coordinator.py
  - tests/test_web_data.py
  - tests/test_cross_repo_contract.py
autonomous: false
requirements: [MODEL-03, MODEL-04]
must_haves:
  truths:
    - "No model-selection correction is applied unless a redacted field diagnostic or a reproducible fixture proves the misclassification."
    - "A correction selects the correct register plan for Navigator 2.0, Navigator 10, and Navigator Pro, and is covered by register-map tests for each family."
    - "If the root cause is in idm-heatpump-api, the integration adopts it ONLY through an exact-pinned, tested API release with passing cross-repository contracts — never a branch or inferred behavior."
    - "The integration never switches model family solely on the basis of a conflicting, unconfirmed display name."
  artifacts:
    - path: custom_components/idm_heatpump/__init__.py
      provides: evidence-gated correction in the detection reconciliation block (only if a confirmed category B/C defect is reproduced)
    - path: tests/fixtures/<anonymous_model_detection>.json
      provides: redacted, reproducible fixture reproducing the confirmed misclassification (if any)
    - path: manifest.json
      provides: exact-pinned idm-heatpump-api version bump (ONLY if category D requires an API release)
  key_links:
    - from: custom_components/idm_heatpump/__init__.py
      to: custom_components/idm_heatpump/adapter_registers.py
      via: "build_filtered_register_map(model_info,...) per family; _NAVIGATOR_10_ONLY_REGISTERS"
      pattern: "MODEL_NAVIGATOR_10|MODEL_NAVIGATOR_20|navigator_family"
---

<objective>
Correct the model-selection defect behind #170 ONLY from reproducible evidence, choosing the right
Navigator family register plan — or, if the defect lives in `idm-heatpump-api`, routing the fix
through an exact-pinned upstream release.

Purpose: A wrong family activates the wrong register plan, which can expose unsupported registers
or hide supported ones. Guessing is riskier than a documented diagnostic. This plan is a
CHECKPOINT: it executes only after plan 03-01 produces redacted field evidence or a reproducible
fixture that confirms a misclassification.
</objective>

<execution_context>
@.planning/phases/03-model-conflict-diagnosis/03-02-PLAN.md
@.planning/phases/03-model-conflict-diagnosis/03-02-SUMMARY.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/PLAN.md
@custom_components/idm_heatpump/__init__.py
@custom_components/idm_heatpump/coordinator.py
@custom_components/idm_heatpump/web_data.py
@custom_components/idm_heatpump/adapter_registers.py
@custom_components/idm_heatpump/diagnostics.py
@manifest.json
@tests/test_init.py
@tests/test_coordinator.py
@tests/test_web_data.py
@tests/test_cross_repo_contract.py
</context>

<interfaces>
Detection reconciliation: `async_setup_entry` (__init__.py:605-803), `_detect_model_info`
(__init__.py:202), `_resolved_model_override` (__init__.py:314). Register plan per family:
`build_filtered_register_map` (adapter_registers.py:94) and `_NAVIGATOR_10_ONLY_REGISTERS`
(adapter_registers.py:21). Web merge: `merge_model_info` (web_data.py:624),
`_firmware_indicates_nav10` (web_data.py:611). API pin: `manifest.json` requirements currently
`idm-heatpump-api[web]==0.8.4`; contract test `EXPECTED_RUNTIME_REQUIREMENTS`
(test_release_contract.py:16-19) and `tests/test_cross_repo_contract.py` must stay green.
</interfaces>

<tasks>

<task type="checkpoint" autonomous="false">
  <name>Task 03-02-00: Evidence checkpoint (BLOCKING)</name>
  <files>N/A</files>
  <read_first>
    - .planning/phases/03-model-conflict-diagnosis/03-01-SUMMARY.md
    - custom_components/idm_heatpump/diagnostics.py
    - custom_components/idm_heatpump/__init__.py
  </read_first>
  <action>
Halt before any correction. Require, from the reporter, redacted evidence:
- Home Assistant diagnostics export of the integration (with host/PIN/serial redacted)
- Debug log from setup up to the conflict warning
- Displayed Navigator version + firmware string
- Value of the manual model override
- Fresh `detect_model()` result and stored detection fields
- Web variant and firmware prefix

Classify into exactly one category using the 03-01 diagnostic:
- A — setpoints were exclusively #172; model warning is a separate non-defect. ACTION: close the model part of #170 as no-defect; do not run 03-02-01/02.
- B — stored detection/override data is stale. ACTION: run 03-02-01 (integration-side reconciliation fix).
- C — integration reconciliation picks the wrong family despite unambiguous evidence. ACTION: run 03-02-01.
- D — `idm-heatpump-api.detect_model()` misclassifies the controller. ACTION: run 03-02-02 (API fix + pin).

Record the category and the redacted evidence reference in 03-02-SUMMARY.md before proceeding. If
evidence is insufficient or category A, STOP and leave MODEL-03/MODEL-04 as a documented open
checkpoint.
  </action>
  <acceptance_criteria>
    - A category (A/B/C/D) decision is recorded in 03-02-SUMMARY.md with a reference to redacted evidence.
    - If category A or evidence is insufficient: no production code is changed and the plan stops here with MODEL-03/MODEL-04 marked open.
    - No unredacted host/PIN/serial appears anywhere in the plan artifacts.
  </acceptance_criteria>
  <verify>
    <manual>Reviewer confirms the category and that evidence is redacted.</manual>
  </verify>
  <done>The correction path is gated on a confirmed, redacted evidence category.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-01: Integration-side evidence-gated family correction (categories B/C only)</name>
  <files>custom_components/idm_heatpump/__init__.py, custom_components/idm_heatpump/coordinator.py, custom_components/idm_heatpump/adapter_registers.py, tests/test_init.py, tests/test_coordinator.py, tests/test_cross_repo_contract.py, tests/fixtures/<anonymous_model_detection>.json</files>
  <read_first>
    - custom_components/idm_heatpump/__init__.py
    - custom_components/idm_heatpump/coordinator.py
    - custom_components/idm_heatpump/adapter_registers.py
    - custom_components/idm_heatpump/web_data.py
    - tests/test_init.py
    - tests/test_coordinator.py
    - tests/test_cross_repo_contract.py
  </read_first>
  <action>
ONLY for confirmed category B or C. Add a redacted, reproducible fixture under
`tests/fixtures/` that reproduces the misclassification (no host/PIN/serial; generic model strings).

Then make the minimal, evidence-backed correction in the reconciliation block
(__init__.py:605-803) and/or `coordinator.async_refresh_web_supplement` (coordinator.py:669) so the
correct family is selected from the confirmed evidence — for example, correcting stale stored
detection (category B) or fixing a reconciliation branch that ignored unambiguous evidence
(category C). Ensure `build_filtered_register_map` (adapter_registers.py:94) receives the corrected
`model_info` so the right per-family register plan is used.

Do NOT switch family on a conflicting display name alone. Do NOT change unique IDs. Keep the
manual override authoritative.

Add/extend tests so that, given the fixture, Navigator 2.0, Navigator 10, and Navigator Pro each
resolve to their expected register plan (extend `tests/test_cross_repo_contract.py` which already
builds Nav10/Nav20 maps). Add an init test asserting the corrected family is selected and persisted
to `entry.data` detection keys.
  </action>
  <acceptance_criteria>
    - A redacted fixture reproduces the pre-fix misclassification and the post-fix correct selection.
    - Navigator 2.0, Navigator 10, and Navigator Pro each yield the expected register plan in `tests/test_cross_repo_contract.py`.
    - No family switch occurs based solely on a conflicting display name.
    - `pytest tests/test_init.py tests/test_coordinator.py tests/test_cross_repo_contract.py -q` exits 0.
    - No host/PIN/serial in the fixture or tests.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_init.py tests/test_coordinator.py tests/test_cross_repo_contract.py -q</automated>
  </verify>
  <done>The integration selects the correct family register plan from confirmed evidence for B/C defects.</done>
</task>

<task type="checkpoint" autonomous="false">
  <name>Task 03-02-02: API-side detection fix and exact-pinned adoption (category D only)</name>
  <files>manifest.json, tests/test_release_contract.py, tests/test_cross_repo_contract.py</files>
  <read_first>
    - manifest.json
    - tests/test_release_contract.py
    - tests/test_cross_repo_contract.py
    - custom_components/idm_heatpump/__init__.py
  </read_first>
  <action>
ONLY for confirmed category D. The fix is made in the separate `idm-heatpump-api` repository with a
reproducible detection test, released as a stable tagged version. Then, in THIS repo:

1. Bump the pin in `manifest.json` `requirements` from `idm-heatpump-api[web]==0.8.4` to the exact
   released version (e.g. `==0.8.5`) — never a branch or `>=`.
2. Update `EXPECTED_RUNTIME_REQUIREMENTS` in `tests/test_release_contract.py` (lines 16-19) to match
   the new exact pin, including the `pymodbus>=3.12.1,<4.0` line unchanged.
3. Ensure `tests/test_cross_repo_contract.py` and the detection tests pass against the pinned
   release.
4. Record in `docs/CHANGELOG.md` under `### Changed` the API pin bump and the reason (model
   detection fix for #170), referencing the upstream release tag.

Do not merge the API pin bump until the upstream release is published and cross-repo contracts pass.
  </action>
  <acceptance_criteria>
    - `manifest.json` pins an exact released `idm-heatpump-api[web]==X.Y.Z` (no branch, no `>=`).
    - `tests/test_release_contract.py` `EXPECTED_RUNTIME_REQUIREMENTS` matches `manifest.json` exactly.
    - `pytest tests/test_release_contract.py tests/test_cross_repo_contract.py -q` exits 0.
    - `docs/CHANGELOG.md` records the pin bump and reason.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_release_contract.py tests/test_cross_repo_contract.py -q</automated>
  </verify>
  <done>Category D is fixed upstream and adopted here only through an exact-pinned, contract-tested release.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                                       | Purpose                                                              |
| ---------------------------------------------------- | -------------------------------------------------------------------- |
| evidence-gated correction in reconciliation block     | Selects correct family from confirmed evidence (B/C)                 |
| `tests/fixtures/<anonymous_model_detection>.json`     | Redacted reproducible misclassification fixture                      |
| per-family register-plan assertions                  | Nav 2.0 / 10 / Pro each get the right plan                           |
| exact `idm-heatpump-api` pin bump (category D only)   | Safe upstream adoption with passing cross-repo contracts            |

</artifacts_produced>

<verification>
Run `pytest tests/ -q`, then `mypy custom_components/idm_heatpump`, then
`ruff check custom_components/idm_heatpump tests` and `ruff format ... --check`. If category D,
additionally confirm the upstream `idm-heatpump-api` release tag exists and its detection test
passes before merging the pin bump.
</verification>

<success_criteria>

- MODEL-03: a model correction is applied only from redacted field evidence or a reproducible fixture, producing the correct register plan for Navigator 2.0/10/Pro.
- MODEL-04: if the defect is in `idm-heatpump-api`, adoption happens only via an exact-pinned release whose detection and cross-repository contract tests pass.
- If no confirming evidence exists, MODEL-03/MODEL-04 remain a documented open checkpoint and no correction is merged.
</success_criteria>

<output>Create `.planning/phases/03-model-conflict-diagnosis/03-02-SUMMARY.md` when done, recording the evidence category and the resolution (or the explicit open-checkpoint status).</output>
