# Plan 06-02 — Integration consumes API sentinels

## Goal

Switch the integration's unused-register classification to the API-declared
`effective_sentinel_values`; demote the numeric heuristic to a non-executing
fallback.

## Requirements covered

- SENT-02: `is_register_unused` uses API-declared sentinels authoritatively.
- SENT-03: writable targets stay callable under a sentinel (#172 preserved);
  read-only sensors of absent hardware stay hidden (no `hide_unused` regression).
- SENT-04: cross-repo + release contracts pin API 0.8.7; 0.8.7 release.

## Tasks

1. Rewrite `is_register_unused` around `effective_sentinel_values` (enum wins
   over sentinel default; NaN/inf guard).
2. Drop `UNUSED_VALUE` / `NEGATIVE_ONE_VALID_REGISTERS` from the hot path.
3. Mirror `effective_sentinel_values` in the conftest stub; align stub
   pump-status / battery_soc with the real API; rework placeholder tests.
4. Pin manifest to `idm-heatpump-api[web]==0.8.7`, bump to 0.8.7, sync docs +
   release-contract test.

## Verification

ruff/mypy clean; pytest 963 passed; live bench: 182 entities, #172 writable GLT
targets created+available, available 146→149 (no regression).
