# Plan 06-01 — API sentinel authority (idm-heatpump-api 0.8.7)

## Goal

Make `idm-heatpump-api` the single authority for "unused / not configured"
sentinel values, replacing the integration's numeric heuristic with a
data-driven, datatype-specific declaration on `RegisterDef`.

## Requirements covered

- SENT-01: `RegisterDef` exposes effective sentinels (explicit or datatype
  default); the catalog covers every case the integration's heuristic handled.

## Tasks

1. Add `DATATYPE_SENTINEL_DEFAULTS` (FLOAT/UCHAR/UINT16/INT16) and
   `RegisterDef.effective_sentinel_values` (explicit wins, else datatype default).
2. Declare explicit pump-status sentinels (exclude `-1` = "off").
3. Regenerate the versioned register-schema fixture.
4. API tests (defaults, override, BOOL/BITFLAG, pump-status, coverage).

## Verification

ruff/mypy clean; pytest 306 passed; live probe 11/0/0 (no behavior change).
