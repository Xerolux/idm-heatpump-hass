# Release Candidate Evidence: 0.17.0-beta.2

Status: `BLOCKED`

Second candidate of the `0.17.0` line. Identical to `0.17.0-beta.1` — the code
audit from `docs/dev/code-audit-2026-09.md` on `modbus-connection` 4.11.1 and
`idm-heatpump-api` 2.0.1 — except for one correction found by the first
candidate's own field use: a register the heat pump does not implement is
explained in the log instead of raised as a repair issue.

`BLOCKED`: the candidate is published and its automated preflight is complete,
but the checks that need a clean Home Assistant and the full seven-day soak are
still `PENDING`. The soak's start observation on real hardware is recorded
below. The soak clock restarts here, because the code changed.

## Candidate

| Field | Value |
|---|---|
| Version / tag | `0.17.0-beta.2` / `v0.17.0-beta.2` |
| Commit SHA | `f0972b0b6c545f4e45bac37c1def6adaa487db42` |
| Published at (UTC) | `2026-09-09T04:40:46Z` |
| Artifact URL | [GitHub release](https://github.com/Xerolux/idm-heatpump-hass/releases/tag/v0.17.0-beta.2) |
| SHA-256 | `055e782184a016d9d53be30262d41fc9622448251056b3112da0ed3660c1c323` (`idm_heatpump.zip`, 354,397 bytes) |
| Runtime requirements | `idm-heatpump-api[web]==2.0.1`, `modbus-connection==4.11.1`, `tmodbus[async-serial]==0.6.2` |

## Test Environment

| Field | Value |
|---|---|
| Tester | |
| Started / completed (UTC) | |
| Home Assistant version / installation type | |
| Heat-pump model | |
| Navigator generation / firmware | |
| Connection path | direct / proxy / other |
| Optional paths in scope | web / zones / cascade / forwarding / none |

Do not record private addresses, PINs, tokens, serial numbers, or customer data.

## Automated Preflight

| Check | Result | Evidence |
|---|---|---|
| Full integration suite and coverage | `PASS` | 1,662 passed; 95.19 % total and 100 % `config_flow.py` coverage |
| Strict mypy | `PASS` | no issues in the integration package, checked against Home Assistant 2026.8.1 sources |
| Ruff and format | `PASS` | all checks passed; 99 files already formatted |
| Documentation language | `PASS` | `scripts/check_documentation_language.py`: all covered documents are English |
| Runtime dependency freshness | `PASS` | `modbus-connection` 4.11.1, `tmodbus` 0.6.2 and `idm-heatpump-api` 2.0.1 are each the newest release on PyPI |
| CI with manifest-pinned API | `PASS` | run 34311753628 on `f0972b0`, Home Assistant 2026.8.1 and 2026.9.0b0 legs |
| CI with API main | `PASS` | same run, both `api-main` legs, including the production Modbus backend smoke test |
| HACS and Hassfest | `PASS` | same run, both validators green on `f0972b0` |
| CodeQL and dependency audit | `PASS` | Security run 34311753565 on `f0972b0`: CodeQL (python, actions) and pip-audit |
| Release workflow and ZIP inspection | `PASS` | Release Management run 34311771258: manifest/tag and changelog-heading validation, pin freshness, and the packed ZIP asserted to carry manifest `0.17.0-beta.2` with the `[web]` extra on the API pin |
| Published checksum | `PASS` | `idm_heatpump.zip.sha256` states `055e7821…c323`, matching the candidate table and the asset digest GitHub recorded |

## Clean Home Assistant Smoke Test

| ID | Check | Result | Evidence / N/A reason |
|---|---|---|---|
| SMOKE-01 | Fresh artifact installation and HA restart | `PENDING` | |
| SMOKE-02 | Config flow and first poll | `PENDING` | |
| SMOKE-03 | Detected model and capabilities match hardware | `PENDING` | |
| SMOKE-04 | Reconfigure and connection test | `PENDING` | |
| SMOKE-05 | Redacted diagnostics | `PENDING` | |
| SMOKE-06 | Reversible safe entity write and read-back | `PENDING` | |
| SMOKE-07 | Original value restored | `PENDING` | |
| SMOKE-08 | Reload, unload, re-enable, and resumed polling | `PENDING` | |
| SMOKE-09 | Upgrade from immediately preceding release (`0.16.2`, and from `0.17.0-beta.1`) | `PENDING` | |
| SMOKE-10 | Optional local web path | `PENDING` | |
| SMOKE-11 | Optional room-temperature forwarding | `PENDING` | |

Allowed result values are `PASS`, `FAIL`, and `N/A`. `N/A` requires a reason.
Any required `PENDING`, `FAIL`, or unjustified `N/A` keeps the overall status
`BLOCKED` or `FAIL`.

## Candidate-Specific Risk

The audit touched code paths that a unit test can only partly speak for. Give
these priority during the live smoke test:

- **Model detection.** Reconciliation of the Modbus probe, stored entry data,
  the web supplement and a manual override moved into `model_resolution.py`.
  Behaviour is meant to be identical; SMOKE-03 and SMOKE-09 are what proves it
  on hardware, in particular for a Navigator 10 whose firmware refuses register
  4108.
- **Setup and teardown.** A failed setup now tears down what it already built,
  background tasks are tied to the config entry, and a poll can no longer
  outlive it. SMOKE-08 covers reload, unload and re-enable.
- **Writes.** The translated-write path is shared by all five writable
  platforms and the domestic hot water boost no longer fights the write pacing.
  SMOKE-06 and SMOKE-07 stay strictly on documented safe registers.
- **Forwarding.** Forwarded room temperatures are now converted to degrees
  Celsius. An installation that forwards a Fahrenheit or Kelvin source is the
  case to watch in SMOKE-11.
- **Runtime pins.** Both moved in `0.17.0-beta.1` and are unchanged here.
- **Repair issues.** After upgrading from `0.17.0-beta.1`, the
  "register not supported" cards must be gone after one restart and must not
  come back; the same information must appear once per register in the log.

## Soak

| Field | Value |
|---|---|
| Candidate unchanged since (UTC) | `2026-09-09T04:40:46Z` |
| Earliest eligible completion (UTC) | `2026-09-16T04:40:46Z` |
| Start observation | `PASS` — read-only live probe ~12 h after publication: running version and all three runtime pins match the candidate manifest, 193 entities registered with 183 available, Modbus polling live on the ~10 s raster, physically plausible readings, zero writes performed |
| Midpoint observation | `PENDING` |
| Final observation | `PENDING` |
| Confirmed blocking regressions | none observed since publication |

Start observation detail (2026-09-09T16:34Z; read-only REST and WebSocket
probe of the maintainer's production Home Assistant 2026.9.1; no service calls,
no entity or register writes; full report and raw evidence retained under
`hardware-proof/`):

- Running integration reported as `0.17.0-beta.2` with `idm-heatpump-api 2.0.1`,
  `modbus-connection 4.11.1` and `tmodbus 0.6.2` — each stated by the
  integration's own runtime-version sensor and matching the candidate manifest
  exactly.
- Environment: heat pump ALM6-15 on a Navigator 10, firmware
  `NAV10_20.24-880-g265e09c4a`, direct Modbus connection, web supplement active.
- 193 entities across all eight platforms; the 10 unavailable ones are optional
  values the plant does not provide (web diagnostics without data, battery SOC
  without a battery, defrost history without a recorded event, idle boost-cancel
  button) — no core entity missing.
- Poll loop demonstrably alive: analog values re-changed on a ~10 s raster
  (outdoor 15.07 °C, DHW top 54.92 °C, heat-pump flow 24.9 °C, spread 2.15 K);
  system state `hot_water_only`/standby with no internal message, plausible for
  the season. A dashboard render confirmed the values reach the Lovelace UI.
- Issue tracker reviewed at the same time: no open items and no updates since
  publication, in particular no reports of data corruption, reconnect loops,
  unsafe writes, or setup regressions.

## Publication

Published as a **prerelease**, not marked latest, so HACS offers it only to
installations that opted into beta versions. The tag was created by the Release
Management workflow (`workflow_dispatch` on `main`, run 34311771258) rather than
by a tag push, which is equivalent: `softprops/action-gh-release` tags the
checked-out commit. Release notes come from the changelog section for this
version.

## Final Verdict

- Overall result: `BLOCKED`
- Maintainer:
- Signed at (UTC):
- Notes / linked issues: the audit work order is `docs/dev/code-audit-2026-09.md`; its open follow-up is E1
