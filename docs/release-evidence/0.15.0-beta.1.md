# Release Candidate Evidence: 0.15.0-beta.1

Status: `BLOCKED`

`BLOCKED` is the correct state for this candidate: it is a beta on the
pre-release channel, and the hardware smoke test, the seven-day soak and the
maintainer signature are all still open. The automated preflight below is
complete and green.

## Candidate

| Field | Value |
|---|---|
| Version / tag | `0.15.0-beta.1` / `v0.15.0-beta.1` |
| Commit SHA | filled in after the merge commit is tagged |
| Published at (UTC) | |
| Artifact URL | |
| SHA-256 | |
| API requirement | `idm-heatpump-api[web]==1.0.1` (unchanged), `modbus-connection==4.8.1`, `tmodbus[async-serial]==0.5.1`, `pymodbus>=3.12.1,<4.0` |

## Test Environment

| Field | Value |
|---|---|
| Tester | open — no hardware run yet |
| Started / completed (UTC) | |
| Home Assistant version / installation type | |
| Heat-pump model | |
| Navigator generation / firmware | |
| Connection path | direct / proxy / other |
| Optional paths in scope | web / zones / cascade / forwarding / none |

Do not record private addresses, PINs, tokens, serial numbers, or customer data.

## Automated Preflight

| Check | Result | Evidence |
|---|---|---|
| CI with manifest-pinned API | `PASS` | PR [#234](https://github.com/Xerolux/idm-heatpump-hass/pull/234), Python 3.14 / HA 2026.8.1, 1189 tests, ruff + strict mypy clean |
| CI with API main | `PASS` | same run, `api-main` matrix leg |
| HACS and Hassfest | `PASS` | Hassfest green on the PR; HACS validation runs on `main` after the merge |
| CodeQL and dependency audit | `PASS` | CodeQL (python, actions) and pip-audit green on the PR |
| Release workflow and ZIP inspection | `PENDING` | runs on the `v0.15.0-beta.1` tag |
| Published checksum | `PENDING` | |

Additional evidence specific to this candidate, both run against the real
`modbus-connection` 4.8.1 (the library ships a pytest plugin that replaces the
test stub whenever the package is installed, in CI as well):

- typed backend errors keep the numeric `exception_code=<N>` marker the
  coordinator's bisect logic matches on, including a vendor-specific code;
- connection pacing measured end to end against a local Modbus TCP server:
  configured 0.1 s produced 0.101 s between requests and a 0.3 s connect delay
  produced 0.301 s before the first request, while an unconfigured endpoint
  stayed at 0.0003 s.

## Clean Home Assistant Smoke Test

| ID | Check | Result | Evidence / N/A reason |
|---|---|---|---|
| SMOKE-01 | Fresh artifact installation and HA restart | `PENDING` | |
| SMOKE-02 | Config flow and first poll | `PENDING` | |
| SMOKE-03 | Detected model and capabilities match hardware | `PENDING` | |
| SMOKE-04 | Reconfigure and connection test | `PENDING` | |
| SMOKE-05 | Redacted diagnostics | `PENDING` | endpoint diagnostics now also carry `message_spacing` and `connect_delay`; the host stays redacted |
| SMOKE-06 | Reversible safe entity write and read-back | `PENDING` | |
| SMOKE-07 | Original value restored | `PENDING` | |
| SMOKE-08 | Reload, unload, re-enable, and resumed polling | `PENDING` | |
| SMOKE-09 | Upgrade from immediately preceding release | `PENDING` | upgrade path from `0.14.1`; both new options default to `0`, so an upgraded entry must poll exactly as before |
| SMOKE-10 | Optional local web path | `PENDING` | unchanged by this candidate |
| SMOKE-11 | Optional room-temperature forwarding | `PENDING` | unchanged by this candidate |

Allowed result values are `PASS`, `FAIL`, and `N/A`. `N/A` requires a reason.
Any required `PENDING`, `FAIL`, or unjustified `N/A` keeps the overall status
`BLOCKED` or `FAIL`.

Worth exercising while the candidate soaks, because it is the only behavior a
user can newly turn on: set the request pause to `0.05` s (or pick the
"unreliable network" profile) and compare poll duration and consecutive
failures against the same installation with the pause at `0`.

## Soak

| Field | Value |
|---|---|
| Candidate unchanged since (UTC) | tag time of `v0.15.0-beta.1` |
| Earliest eligible completion (UTC) | tag time + seven consecutive 24-hour periods |
| Start observation | `PENDING` |
| Midpoint observation | `PENDING` |
| Final observation | `PENDING` |
| Confirmed blocking regressions | none reported yet |

The runtime dependency of the direct Modbus socket changed with this candidate,
so the soak clock restarts here regardless of the preceding `0.14.1` runtime.

## Final Verdict

- Overall result: `BLOCKED`
- Maintainer:
- Signed at (UTC):
- Notes / linked issues: PR [#234](https://github.com/Xerolux/idm-heatpump-hass/pull/234). Open before a stable `0.15.0`: hardware smoke test, seven-day soak, maintainer signature.
