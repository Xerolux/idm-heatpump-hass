# Release Candidate Evidence: 0.17.0-beta.3

Status: `PENDING`

This beta fixes model persistence after confirmed web correction and prevents
cached client information from selecting a conflicting register map after an
inconclusive probe. It includes PR #313. The runtime pins are unchanged.

## Preflight

- Four regression cases reproduced the defects before correction.
- Full local suite: 1664 passed, 2 skipped; total coverage 95.19%, config flow 100%.
- Strict mypy, Ruff lint and formatting passed.
- PR #313: all four HA/API quality legs, Hassfest, CodeQL and pip-audit passed.
- Release metadata, dependency and documentation tests: 70 passed, 2 skipped.
- PyPI pin freshness check passed on 2026-09-11.

## Publication and deployment

- Published at 2026-09-11T06:40:23Z as a prerelease.
- Tag commit: `e6b0294cc7979f7ac8b0401051d75b763d9ad7ec`.
- [Release artifact](https://github.com/Xerolux/idm-heatpump-hass/releases/tag/v0.17.0-beta.3):
  `idm_heatpump.zip`, 354306 bytes.
- SHA-256: `b3f114a276c3b073aadc64aea17ad353e0e2b03c7027c8ce236f04a0aa99d6d8`.
  Downloaded bytes match the checksum asset and GitHub's recorded digest.
  Every packaged integration file matches the tagged checkout.
- Release workflow 34570805340 and main CI 34570799187 passed, including
  HACS/Hassfest and the production backend import smoke test.
- A local Home Assistant configuration backup was completed before the upgrade
  (206.07 MB). Configuration validation returned valid with no warnings.
- HACS installed the exact beta.3 version, followed by a Home Assistant Core
  restart. Diagnostics confirm the running integration is 0.17.0-beta.3 on
  Home Assistant 2026.9.1, with unchanged runtime dependencies.
- The initial existing-installation upgrade observation confirms a loaded entry,
  Navigator 10, no model conflict, working Modbus and web supplement, and
  valid heating-circuit and hot-water states. Continued polling is checked below.
- No manual heat-pump or KNX write test was performed. Existing configured
  automation resumes normally after the authorized Core restart.
- No IDM-specific warning/error appeared in the initial post-restart system log.
  A tmodbus unexpected-response warning for unit 3 was already present before
  the upgrade and recurred; its originating integration is not established.
  A dismissed legacy firmware-register repair record remains in diagnostics.

## Stable-release gate

This candidate starts a new seven-day soak. Clean-instance lifecycle validation,
separate hardware write validation and maintainer sign-off remain pending.
The existing-installation upgrade test is not a clean-instance smoke test.
No physical write test is authorized by the deployment request.

## Repository cleanup

The merged model-fix branch was removed. Existing untracked GUI screenshots are
archived outside the repository. Historical changelog and release evidence are
retained because they document published artifacts and regression decisions.

## Initial live observation

At 2026-09-11T06:44:48Z, the beta.3 poll counter had advanced from 3 to 12 with
zero total/consecutive failures. The final poll took 0.013 seconds. Runtime and
stored model both report Navigator 10; the web supplement remains available.
No IDM logger warning/error was present. The dashboard and histories rendered
with current temperatures and the earlier display fixes intact. The SolarEdge
integration also completed startup; its pre-existing warning is not classified
as an IDM regression.

This is a short post-upgrade observation, not a completed soak or a guarantee
that every optional feature has been exercised.
