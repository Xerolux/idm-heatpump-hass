name: Capture PR 141 Tests

on:
  push:
    branches:
      - feat/web-binary-sensors-v2

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/web-binary-sensors-v2
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - name: Install test environment
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install "homeassistant==2026.5.0" pytest pytest-asyncio pytest-homeassistant-custom-component
          python - <<'PY'
          import json
          import subprocess
          import sys

          with open("custom_components/idm_heatpump/manifest.json", encoding="utf-8") as manifest_file:
              requirements = json.load(manifest_file)["requirements"]
          subprocess.check_call([sys.executable, "-m", "pip", "install", *requirements])
          PY
      - name: Capture focused tests
        run: |
          pytest tests/test_web_binary_sensors.py -v --tb=short > pytest-pr-141.txt 2>&1 || true
      - name: Commit output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pytest-pr-141.txt
          git commit -m "test: capture focused PR 141 failures"
          git push origin HEAD:feat/web-binary-sensors-v2
